#!/usr/bin/env python3
"""Sum a list of floating-point values."""

from typing import List


def sum_list(input_list: List[float]) -> float:
    """Return the sum of the values in ``input_list``."""
    return sum(input_list)
